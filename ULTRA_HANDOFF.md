# УЛЬТРА-HANDOFF: mecanum_nucleo

Дата обновления: 2026-04-20

## 0) Кратко о цели
- Платформа на всенаправленных колесах (mecanum), 4 TT-мотора.
- Контроллер: Nucleo L496ZG.
- Драйверы: 2x L9110S (левый блок FL+BL, правый блок FR+BR).
- Финальное управление: PS2 gamepad.
- Текущий этап: диагностический режим из-за проблем левого драйвера/силовой части.

## 1) Среда и сборка
- Zephyr: 3.1.0
- Toolchain: Zephyr SDK 0.17.4
- Build flow: `cmake + make` (не `west build`)
- Flash: `west flash -r jlink`
- Логи: Serial Port Terminal (COM/tty ACM)

Команды:
- `cmake -DBOARD=nucleo_l496zg ..`
- `make -j4`
- `west flash -r jlink`

Если ccache мешает:
- `CCACHE_DISABLE=1 make -j4`
- `export CCACHE_TEMPDIR=/tmp/ccache-tmp`

## 2) Пины и аппаратная карта (по текущему проекту)
Моторы:
- FL: DIR PB13, PWM TIM1_CH1 (PE9)
- FR: DIR PE10, PWM TIM1_CH3 (PE13)
- BL: DIR PB8, PWM TIM2_CH1 (PA0)
- BR: DIR PA5, PWM TIM17_CH1 (PA7)

Датчики скорости (одноканальные ИК):
- FL: PB6 (D71)
- FR: PC6 (D16)
- BL: PE8 (D42)
- BR: PD3 (D55)

PS2 (битбанг GPIO):
- CLK: PC8
- CMD: PC12
- ATT/SEL: PC10
- DAT: PC11

## 3) Что уже исправлено в коде
### 3.1 PWM API (Zephyr 3.1)
- В `zephyr_motor.c` заменены вызовы `pwm_set_pulse_dt(...)` на `pwm_set(...)`.
- Добавлен include `<stdlib.h>` (для `abs` в motor module).

### 3.2 Линковка и математика в accelmotor
- В `zephyr_accelmotor.c` убраны зависимости от libc `fabs/abs`.
- Введены локальные функции `iabs32/fabs32`.
- Это убрало undefined references на линковке.

### 3.3 Потоки и порядок запуска
- Убрано автосоздание через `K_THREAD_DEFINE` для основной логики.
- Потоки создаются через `k_thread_create` только после успешного `init_hardware()`.

### 3.4 Синхронизация
- Добавлен mutex (`control_lock`) между motor/ps2 потоками для общих состояний.

### 3.5 Overlay/DTS
- Overlay приведен к рабочему состоянию для Zephyr 3.1.
- В `chosen` выставлены:
  - `zephyr,console = &usart3`
  - `zephyr,shell-uart = &usart3`
- Включен `pwm17` под TIM17 для BR.

### 3.6 Kconfig cleanup
- Из `prj.conf` удалены несовместимые символы:
  - `CONFIG_UART_CONSOLE_ON_DEV_NAME`
  - `CONFIG_TIMER`

## 4) Модель управления драйверами L9110S
- Драйверы моторов: 2x L9110S.
- Для каждого канала используется 2 управляющих сигнала:
  - один PWM,
  - один DIR.
- Для текущей схемы:
  - x-1A = PWM-вход канала,
  - x-1B = DIR-вход канала.
- Исторические упоминания 3-пиновой модели `DIR1/DIR2/PWM` больше не актуальны для текущего проекта.

## 5) Текущий режим (диагностика)
В `main.c` активен:
- `#define MOTOR_AUTOTEST_NO_PS2 1`

Эффект режима:
- PS2 отключен (инициализация пропущена).
- Контур PID/энкодеров отключен в `init_hardware()`.
- `motor_control_thread` не управляет моторами.
- Автотест делает прямое управление через `zephyr_motor_set_speed()`:
  - ~2 сек FORWARD
  - ~2 сек BACKWARD
  - ~2 сек STOP
  - цикл повторяется

## 6) Что сделали в этом чате
### 6.1 По подключению/питанию
- Исправлена полярность электролитов:
  - короткая ножка = GND,
  - длинная ножка = плюс питания.
- Подтверждена общая земля для Nucleo, L9110S, PS2 и датчиков.
- Рабочая схема на этапе отладки: USB питает логику Nucleo, внешний БП питает силовую часть.

### 6.2 По коду
- Моторный модуль приведен к явной двухпиновой модели L9110S.
- `main.c` синхронизирован с реальной схемой: у каждого мотора теперь один DIR pin и один PWM pin.
- Автотест оставлен активным через `MOTOR_AUTOTEST_NO_PS2=1`.

## 7) Наблюдаемое состояние железа
- Левая сторона:
  - по одному мотору работает,
  - два левых мотора вместе тоже работают после снижения стартового duty и щадящих параметров БП.
- Правая сторона:
  - при 5V / 1A задний мотор крутится,
  - передний в основном дергается.
- Главный текущий риск сместился в сторону правого переднего канала/мотора под нагрузкой.

## 8) Почему код местами серый в VS Code
- Это inactive branch препроцессора (`#if/#else`) из-за `MOTOR_AUTOTEST_NO_PS2=1`.
- При `MOTOR_AUTOTEST_NO_PS2=0` активируется PS2-код, автотест-секция станет неактивной.

## 9) План дальнейших действий
1. Правый блок: тест одного мотора на канал.
2. Поменять моторы местами между правыми каналами.
3. Поменять местами только управляющие входы каналов.
4. Понять по результатам: проблема в моторе/механике или в канале драйвера/проводке.
5. Держать щадящие настройки на старте:
   - около 5.0V,
   - токовый лимит от 1.0A,
   - сниженный стартовый duty.
6. После стабильности правой стороны — прогон всех 4 моторов.
7. Только потом вернуть PS2-режим и перейти к тюнингу.

## 10) Как должна работать PS2 логика (режим 0)
D-pad (position mode, шагами):
- LEFT: FL+, FR-, BL-, BR+
- RIGHT: FL-, FR+, BL+, BR-
- UP: все +
- DOWN: все -

Sticks (speed mode):
- трансляция через LX/LY
- вращение добавляется через RX/RY
- значения ограничиваются `MAX_SPEED`

Примечание:
- Поворот от RX/RY может быть неинтуитивным (обычно берут один RX); возможно понадобится тюнинг маппинга.

## 11) Файлы, которые точно изменялись
- `mecanum/src/main.c`
- `mecanum/src/zephyr_motor.c`
- `mecanum/src/zephyr_motor.h`
- `mecanum/src/zephyr_accelmotor.c`
- `mecanum/prj.conf`
- `mecanum/nucleo_l496zg.overlay`
- `ULTRA_HANDOFF.md`

## 12) Риски/узкие места
- Основной риск: силовая часть.
- Пусковой ток двух моторов на одной стороне может давать C.C даже при корректной логике.
- Макетка и тонкие силовые перемычки могут давать просадки.

## 13) Контрольный чеклист
- `cmake` проходит без DTS/Kconfig ошибок.
- `make` доходит до `zephyr.elf`.
- `west flash -r jlink` успешно.
- В автотесте левый и правый блок крутят предсказуемо по циклу.
- Нет повторяющегося C.C на БП при штатном включении.

## 14) Точное действие для возврата в финальный сценарий
В `main.c` поменять:
- `#define MOTOR_AUTOTEST_NO_PS2 1`
на
- `#define MOTOR_AUTOTEST_NO_PS2 0`

После этого: rebuild + flash, затем верификация PS2 и кинематики.
